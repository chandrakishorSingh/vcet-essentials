import {LectureModel} from '../../models/lecture.model';

const timetable: LectureModel[] = [
    // Monday

    // Tuesday

    // Wednesday

    // Thursday

    // Friday
];

export default timetable;
